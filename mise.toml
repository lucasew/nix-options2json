[tools]
shfmt = "3.7.0"
shellcheck = "0.9.0"

[tasks]
lint = "shellcheck bin/dump-options"
fmt = "shfmt -w bin/dump-options"
test = "if ! command -v nix-instantiate > /dev/null; then echo 'Skipping test: nix-instantiate not found'; exit 0; fi; ./bin/dump-options \"{}\""
